# FastAPI backend for NutriVision mobile app
# Include full project deps (torch/transformers/peft/etc.) required by inference.
-r ../requirements.txt

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
pydantic>=2.5.0
Pillow>=10.0.0
